import { WorkbenchApp } from "@/components/workbench/WorkbenchApp";

export const metadata = { title: "Buildzone — Tandwielenlab" };

export default function BuildzonePage() {
  return <WorkbenchApp mode="build" />;
}
